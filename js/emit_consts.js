const ROMAN = 1;
const ITALIC = 2;
const SYMBOL = 3;
const EXPR = 4;
const PAREN = 5;
const BRACK = 6;
const SUPERSCRIPT = 7;
const SUBSCRIPT = 8;
const FRACTION = 9;
const TABLE = 10;
const SPACE = 11;

const FONT_SIZE = 20
//const SCRIPT_FONT_SIZE = 14;

const FONT_HEIGHT = Math.round(0.8 * FONT_SIZE);
const FONT_DEPTH = Math.round(0.2 * FONT_SIZE);
const FONT_WIDTH = Math.round(0.6 * FONT_SIZE);
const X_HEIGHT = Math.round(0.3 * FONT_SIZE);
const THIN_SPACE_WIDTH = Math.round(0.2 * FONT_SIZE);
const THICK_SPACE_WIDTH = Math.round(0.4 * FONT_SIZE);
const FRAC_PAD = Math.round(0.1 * FONT_SIZE);
const CELL_PAD = Math.round(0.1 * FONT_SIZE);

//const SCRIPT_FONT_HEIGHT = Math.round(0.8 * SCRIPT_FONT_SIZE);
//const SCRIPT_FONT_DEPTH = Math.round(0.2 * SCRIPT_FONT_SIZE);
//const SCRIPT_FONT_WIDTH = Math.round(0.6 * SCRIPT_FONT_SIZE);
//const SCRIPT_X_HEIGHT = Math.round(0.3 * SCRIPT_FONT_SIZE);
//const SCRIPT_THIN_SPACE_WIDTH = Math.round(0.2 * SCRIPT_FONT_SIZE);
//const SCRIPT_THICK_SPACE_WIDTH = Math.round(0.4 * SCRIPT_FONT_SIZE);
