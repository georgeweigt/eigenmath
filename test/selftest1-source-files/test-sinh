"Testing sinh"

clear

check(sinh(0) == 0)
check(sinh(-x) == -sinh(x))
check(sinh(arcsinh(x)) == x)

for(x,-12,12,
  d = float(sinh(x/12)) - sinh(float(x/12)),
  check(abs(d) < 1.0 10^(-6))
)

check(infixform(sinh(0.5 + 0.5 i)) == "0.457304 + 0.540613 i")
