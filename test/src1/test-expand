
-- FIXME

--T = expand((x^2+5*x+2)/(2*(x+1)),x)
--check(infixform(T) == "1/2 x - 1 / (x + 1) + 2")

--f = x / 2 / (x + 1)
--T = expand(f)
--check(rationalize(T) == f)

--f = x / 2 / (x + 1) / (x + 2)
--T = expand(f)
--check(rationalize(T) == f)

--f = x / 2 / (x + 1) / (x + 2) / (x + 3)
--T = expand(f)
--check(rationalize(T) == f)
